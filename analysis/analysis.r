library(dplyr)

renv::snapshot()
  #scans the defined folder (or working directory) for usage of any packages anywhere
  #to then save all the necessary information to reproduce into the renv.lock file



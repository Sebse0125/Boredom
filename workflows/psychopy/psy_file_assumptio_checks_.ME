pre_preperation of psychopy files to enable clean future workflows and keeping raw files complete

setting directory and working folder
    find all files
    check duplicates
    copy them to new folder for further pre-processing
standardizing file names
    use helper to consistently find Condition, Blocl and ID in files
        find all files in s_files (standardised files) folder
        finding accurate data on ID, Block and Condition
        standardizing file names according to those inputs
            -> this will later simplify column naming because of contradictory information at certain points
            -> this ensures, that file names are in fact correct and that data extracted from files will be placed in correct spot